import {Card, CardContent, CardHeader} from "@material-ui/core";

const AdminPanel = () => (
    <Card>
        <CardHeader title={"Wellcome to Admin Panel"}/>
        <CardContent>This is the board to control all.</CardContent>
    </Card>
);
export default AdminPanel;